VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""
VITE_SHOPIFY_STOREFRONT_TOKEN=""

# --- Local scripts only (no VITE_ prefix — never exposed to browser) ---
# Preferred: Edge verify — no service role in .env (Lovable hides it; Edge gets it at runtime).
TRUTH_VERIFY_SECRET=""
# Optional: only if Lovable gives you the real service_role JWT (not the anon key).
# SUPABASE_SERVICE_ROLE_KEY=""

# --- Supabase Edge secrets (set in Lovable Cloud, not Vite) ---
# FANFUEL_HUB_KEY=""  (server-to-server; Telegram CC, CCA internal proxies)
# FROM_EMAIL="pitches@fendifrost.com"
# REPLY_TO_EMAIL="fendifrost@gmail.com"
# RESEND_API_KEY=""
# FIRECRAWL_API_KEY=""
# HUNTER_API_KEY=""  (optional curator email fallback from website domain)
# STATS_CRON_SECRET=""
# ARTIST_USER_ID=""
# INNER_CIRCLE_BOT_TOKEN=""
# INNER_CIRCLE_BOT_USERNAME="FendiInnerCircle"
# INNER_CIRCLE_WEBHOOK_SECRET=""
